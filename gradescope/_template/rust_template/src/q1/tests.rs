use super::*;

// ---------------------------------------------------------------------------
// IO tests — visible to students immediately
// Each #[test] fn name here must appear verbatim in config.json under "name".
// kind: "io"  → description shown to student on pass/fail
// ---------------------------------------------------------------------------

#[test]
fn test_TODO_basic() {
    // TODO: replace with a real assertion
    // assert_eq!(todo_function_one(...), expected);
    todo!()
}

// ---------------------------------------------------------------------------
// Structural tests — hidden until after the due date
// kind: "structural"  → only PASS / FAIL shown, no description
// ---------------------------------------------------------------------------

#[test]
fn test_TODO_edge() {
    // TODO: replace with a real assertion
    // assert_eq!(todo_function_one(...), expected);
    todo!()
}
